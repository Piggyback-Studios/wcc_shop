//  general type the other users will extend from
// email and hashed password, maybe some other info

export type User = {
  id: string;
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};
